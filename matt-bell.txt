https://codepen.io/mattbell42/pen/WNwVYmG?editors=1100

    1. What is Semantic HTML? (markup used to give the HTML meaning
    2. What is HTML used for? (it's used to structure web pages)
    3. What is an attribute and where do we put it? (they are put in the start tag and provide additional information about an element)
    4. What is the h1 tag used for? How many times should I use it on a page? (it's used for the title of the page and con only be used once)
    5. Name two tags that have required attributes (<img> and <a>)
    6. What do we put in the head of our HTML document? (the sources for fonts, colors, and images)
    7. What is an id? (used to specify a unique id for an element)
    8. What elements can I add an id to? (any)
    9. How many times can I use the same id on a page? (an ID must be specefic to a certian element)
    10. What is a class? (used to specify a class of elements)
    11. What elements can I add a class to? (any)
    12. How many times can I use the same class on a page? (as many times as neccesary)
    13. How do I get my link to open in a new tab? (target="_blank" after the link in the <a> tag)
    14. What is the alt attribute in the image tag used for? (it provides an alternate text for an image in case the image cannot be displayed or for a screen reader)
    15. How do I reference an id? (#id)
    16. What is the difference between a section and a div (section means that the content inside is grouped, div has no meaning)
    17. What is CSS used for? (it's used for describing the presentation of a web page)
    18. How to we select an element? Example - every h2 on the page (h2{})
    19. What is the difference between a class and an id? - Give me an example of when I might use each one (class can be used when describing multiple elements, wheras an ID must be unique to a single element.)
    
    20. How do we select classes in CSS? (.class{})
    21. How do we select a p element with a single class of “human””? (.human p{})
    22. What is a parent child selector? When would this be useful? (used to select elements with a speceific parent,
    it would be useful when styling multiple all paragraphs with a div element)
    23. How do you select all links within a div with the class of sidebar? (div > .sidebar)
    24. What is a pseudo selector? (used to design a special state of an element)
    25. What do we use the change the spacing between lines? (line-height:)
    26. What do we use to change the spacing between letters? (letter-spacing:)
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? (text-tansform:capitalize; text-transform:lowercase; text-transform:uppercase;)
    28. How do I add a 1px border around my div that is dotted and black? (div{ border:1px dotted black;})
    29. How do I select everything on the page? (*)
    30. How do I write a comment in CSS? (/* this is how you write a comment*/)
    31. How do I find out what file I am in, when I am using the command line? 
    32. Using the command line - how do I see a list of files/folders in my current folder? (ls)
    33. How do I remove a file via the command line? Why do I have to be careful with this?  (git rm, you can delete important files from your computer)
    34. Why should I use version control? (you can keep track of changes you've made and revert back to different versions)
    35. How often should I commit to github? (every time you make a significant change)
    36. What is the command we would use to push our repo up to github? (git push)
    37. Walk me through Lambda's git flow. (fork-clone-cd into the new repo-create your own branch-git add to add changes-git commit to save changes-git push to the github repo)